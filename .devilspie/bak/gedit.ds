( if 
( and 
( is ( application_name ) "gedit" )
( matches ( window_class ) "Gedit" )
) 
( begin 
( maximize )
;(spawn_async "gnome-terminal --working-directory=$GEDIT_CURRENT_DOCUMENT_DIR &")
( println "match" )
)
)
